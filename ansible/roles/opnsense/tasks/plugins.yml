---
- name: Pull updates
  oxlorg.opnsense.system:
    action: 'update'

- name: Install plugins
  tags: plugins
  oxlorg.opnsense.package:
    name: "{{ item }}"
    action: install
  loop: "{{ plugins }}"

