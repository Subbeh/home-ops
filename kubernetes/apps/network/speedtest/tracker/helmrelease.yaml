---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: speedtest-tracker
spec:
  chartRef:
    kind: OCIRepository
    name: speedtest-tracker
  interval: 1h
  values:
    controllers:
      speedtest-tracker:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: lscr.io/linuxserver/speedtest-tracker
              tag: 1.7.3@sha256:d45d54ec3836f487817fb42d44a8b7ac73e3442cb66fd142bb795cbc19592745
            envFrom:
              - secretRef:
                  name: speedtest-tracker-secret
            env:
              SPEEDTEST_SCHEDULE: "0 * * * *"
              SPEEDTEST_SERVERS: "60412,25134,68925,37688"
              APP_URL: https://speedtracker.sbbh.cloud
              APP_TIMEZONE: &tz Australia/Melbourne
              DISPLAY_TIMEZONE: *tz
              DB_CONNECTION: sqlite
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 512Mi
    service:
      app:
        ports:
          http:
            port: 80
    route:
      app:
        hostnames:
          - "speedtracker.sbbh.cloud"
        parentRefs:
          - name: envoy-internal
            namespace: network
    persistence:
      config:
        existingClaim: "{{ .Release.Name }}"
